# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0

# LLM — Groq free tier (get key at https://console.groq.com)
groq==0.11.0

# Semantic retrieval — optional FAISS backend
# On Render free tier, RETRIEVAL_BACKEND=tfidf (no install needed)
# For local use with FAISS: these are installed but only used if RETRIEVAL_BACKEND=faiss
faiss-cpu==1.9.0
sentence-transformers==3.1.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
requests==2.32.3
